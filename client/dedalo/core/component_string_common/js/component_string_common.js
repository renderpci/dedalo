// @license magnet:?xt=urn:btih:0b31508aeb0634b347b8270c7bee4d411b5d4109&dn=agpl-3.0.txt AGPL-3.0

/**
* COMPONENT_STRING_COMMON
* Client-side stub module for the component_string_common base class.
*
* The PHP counterpart (class.component_string_common.php) provides the abstract
* base for all string-typed components (component_input_text, component_text_area,
* component_email, etc.).  This JS file is a placeholder reserved for any
* shared client-side behaviour that future string-component subclasses may need
* to import.  At the time of writing, all string-component UI logic lives
* directly in each concrete component's own JS file.
*
* When client-side shared string logic is added here it should be exported as
* named ES module exports so that concrete subclasses can import selectively.
*/

// @license-end
