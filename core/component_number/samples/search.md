---
description: Provides samples of component parsed results for number components.
---

# component number

This file provides samples of parsed results for components extending the `component_number` class and using the `search_component_number` trait.
The results are generated by the `resolve_query_object_sql` method.
Use component_tipo `test211` for samples.

### 1. Operator !* (Is Empty)
Param $query_object:
```json
{
    "q": "only_operator",
    "q_operator": "!*",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "only_operator",
    "q_operator": "!*",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "$.test211[*] ? (@.value != null)"
    },
    "sentence": "(te3.number->'test211' IS NULL OR NOT te3.number @? (_Q1_)::jsonpath)"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number->'test211' IS NULL OR NOT te3.number @? ('$.test211[*] ? (@.value != null)')::jsonpath)
```

### 2. Operator * (Not Empty)
Param $query_object:
```json
{
    "q": "only_operator",
    "q_operator": "*",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "only_operator",
    "q_operator": "*",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "$.test211[*].value ? (@ != null)"
    },
    "sentence": "te3.number @? (_Q1_)::jsonpath"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
te3.number @? ('$.test211[*].value ? (@ != null)')::jsonpath
```

### 3. Between (value1...value2)
Param $query_object:
```json
{
    "q": "10...20",
    "q_operator": null,
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "10...20",
    "q_operator": null,
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "10",
        "_Q2_": "20"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric >= (_Q1_)::numeric\n    AND (elem->>'value')::numeric <= (_Q2_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric >= ('10')::numeric
    AND (elem->>'value')::numeric <= ('20')::numeric
 )
```

### 4. Operator >= (Bigger or Equal)
Param $query_object:
```json
{
    "q": "15",
    "q_operator": ">=",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "15",
    "q_operator": ">=",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "15"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric >= (_Q1_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric >= ('15')::numeric
 )
```

### 5. Operator <= (Smaller or Equal)
Param $query_object:
```json
{
    "q": "15",
    "q_operator": "<=",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "15",
    "q_operator": "<=",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "15"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric <= (_Q1_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric <= ('15')::numeric
 )
```

### 6. Operator > (Bigger Than)
Param $query_object:
```json
{
    "q": "15",
    "q_operator": ">",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "15",
    "q_operator": ">",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "15"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric > (_Q1_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric > ('15')::numeric
 )
```

### 7. Operator < (Smaller Than)
Param $query_object:
```json
{
    "q": "15",
    "q_operator": "<",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "15",
    "q_operator": "<",
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "15"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric < (_Q1_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric < ('15')::numeric
 )
```

### 8. Equality (Default)
Param $query_object:
```json
{
    "q": "42",
    "q_operator": null,
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "42",
    "q_operator": null,
    "path": [
        {
            "name": "number",
            "model": "component_number",
            "section_tipo": "test3",
            "component_tipo": "test211"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "number",
    "unaccent": false,
    "params": {
        "_Q1_": "42"
    },
    "sentence": "(te3.number @? '$.test211[*]') AND EXISTS (\n  SELECT 1\n  FROM jsonb_array_elements(te3.number->'test211') AS elem\n  WHERE (elem->>'value')::numeric = (_Q1_)::numeric\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.number @? '$.test211[*]') AND EXISTS (
  SELECT 1
  FROM jsonb_array_elements(te3.number->'test211') AS elem
  WHERE (elem->>'value')::numeric = ('42')::numeric
 )
```
