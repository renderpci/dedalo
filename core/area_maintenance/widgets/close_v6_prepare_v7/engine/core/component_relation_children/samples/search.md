---
description: Provides samples of component parsed results for component_relation_children.
---

# component_relation_children

This file provides samples of parsed results for components using the `search_component_relation_children` trait.
The results are generated by the `resolve_query_object_sql` method.
Use component_tipo `test201` for samples (assumed to relate to parent tipo `test71`).

### 1. Operator !* (Is Empty)
Param $query_object:
```json
{
    "q": "only_operator",
    "q_operator": "!*",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "only_operator",
    "q_operator": "!*",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "jsonb",
    "params": {
        "_Q1_": "test71"
    },
    "sentence": "NOT EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 
WHERE NOT EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
)
```

### 2. Operator * (Not Empty)
Param $query_object:
```json
{
    "q": "only_operator",
    "q_operator": "*",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": "only_operator",
    "q_operator": "*",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "jsonb",
    "params": {
        "_Q1_": "test71"
    },
    "sentence": "EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 
WHERE EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
)
```

### 3. Operator == (Contain / Default)
Param $query_object:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "==",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "==",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "jsonb",
    "params": {
        "_Q1_": "test71",
        "_Q2_": "{\"section_id\":\"51\",\"section_tipo\":\"test_parent\"}"
    },
    "sentence": "EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text AND sub.section_id::text = (_Q2_::jsonb->>'section_id') AND sub.section_tipo = (_Q2_::jsonb->>'section_tipo') )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 
WHERE EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
      AND sub.section_id::text = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_id') 
      AND sub.section_tipo = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_tipo')
)
```

### 4. Operator != (Different)
Param $query_object:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "!=",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "!=",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "jsonb",
    "params": {
        "_Q1_": "test71",
        "_Q2_": "{\"section_id\":\"51\",\"section_tipo\":\"test_parent\"}"
    },
    "sentence": "EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text ) AND NOT EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text AND sub.section_id::text = (_Q2_::jsonb->>'section_id') AND sub.section_tipo = (_Q2_::jsonb->>'section_tipo') )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 
WHERE EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
) AND NOT EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
      AND sub.section_id::text = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_id') 
      AND sub.section_tipo = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_tipo')
)
```

### 5. Operator !== (Strict Different)
Param $query_object:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "!==",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test"
}
```
Parsed result:
```json
{
    "q": {
        "section_id": "51",
        "section_tipo": "test_parent"
    },
    "q_operator": "!==",
    "path": [
        {
            "name": "relation_children",
            "model": "component_relation_children",
            "section_tipo": "test3",
            "component_tipo": "test201"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "type": "jsonb",
    "params": {
        "_Q1_": "test71",
        "_Q2_": "{\"section_id\":\"51\",\"section_tipo\":\"test_parent\"}"
    },
    "sentence": "NOT EXISTS ( SELECT 1 FROM \"matrix_test\" AS sub CROSS JOIN LATERAL jsonb_array_elements( CASE WHEN jsonb_typeof(sub.relation->_Q1_) = 'array' THEN sub.relation->_Q1_ ELSE jsonb_build_array(sub.relation->_Q1_) END ) AS elem WHERE sub.relation ? _Q1_ AND elem->>'section_id' = te3.section_id::text AND sub.section_id::text = (_Q2_::jsonb->>'section_id') AND sub.section_tipo = (_Q2_::jsonb->>'section_tipo') )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 
WHERE NOT EXISTS ( 
    SELECT 1 FROM "matrix_test" AS sub 
    CROSS JOIN LATERAL jsonb_array_elements( 
        CASE 
            WHEN jsonb_typeof(sub.relation->'test71') = 'array' THEN sub.relation->'test71' 
            ELSE jsonb_build_array(sub.relation->'test71') 
        END 
    ) AS elem 
    WHERE sub.relation ? 'test71' 
      AND elem->>'section_id' = te3.section_id::text 
      AND sub.section_id::text = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_id') 
      AND sub.section_tipo = ('{"section_id":"51","section_tipo":"test_parent"}'::jsonb->>'section_tipo') 
)
```
