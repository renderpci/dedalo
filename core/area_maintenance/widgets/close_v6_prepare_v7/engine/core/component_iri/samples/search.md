---
description: Provides samples of component parsed results for IRI components.
---

# Component IRI

This file provides samples of parsed results for components extending the `component_iri` class and using the `search_component_iri` trait.
The results are generated by the `resolve_query_object_sql` method.
Use component_tipo `test140` for samples.

### 1. Operator !* (Is Empty) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "!*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "!*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "$.test140[*].iri ? (@ != \"\" && @ != null)"
    },
    "sentence": "(te3.iri IS NULL OR NOT (te3.iri @? (_Q1_)::jsonpath))"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri IS NULL OR NOT (te3.iri @? ('$.test140[*].iri ? (@ != "" && @ != null)')::jsonpath))
```

### 1b. Operator !* (Is Empty) (lang = 'lg-eng')
Param $query_object:
```json
{
    "q": [
        {
            "value": "!*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "lg-eng"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "!*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "lg-eng",
    "type": "string",
    "params": {
        "_Q1_": "$.test140[*] ? (@.lang == \"lg-eng\" && @.iri != \"\" && @.iri != null)"
    },
    "sentence": "(te3.iri IS NULL OR NOT (te3.iri @? (_Q1_)::jsonpath))"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri IS NULL OR NOT (te3.iri @? ('$.test140[*] ? (@.lang == "lg-eng" && @.iri != "" && @.iri != null)')::jsonpath))
```

### 2. Operator * (Not Empty) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "$.test140[*].iri ? (@ != \"\" && @ != null)"
    },
    "sentence": "te3.iri @? (_Q1_)::jsonpath"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? ('$.test140[*].iri ? (@ != "" && @ != null)')::jsonpath)
```

### 3. Operator != (Is Different) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "!=http://example.org"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "!=http://example.org"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "http://example.org"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND NOT EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') = f_unaccent(_Q1_) )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND NOT EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') = f_unaccent('http://example.org') )
```

### 4. Operator - (Does Not Contain) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "-example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "-example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "example"
    },
    "sentence": "NOT EXISTS (\n  SELECT 1\n  FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem\n  WHERE elem->>'iri' IS NOT NULL AND f_unaccent(elem->>'iri') ~* f_unaccent(_Q1_)\n )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND NOT EXISTS (
  SELECT 1
  FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem
  WHERE elem->>'iri' IS NOT NULL AND f_unaccent(elem->>'iri') ~* f_unaccent('example')
 )
```

### 5. Operator = (Similar To) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "=example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "=example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "example"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* f_unaccent(_Q1_) )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* f_unaccent('example') )
```

### 6. Begins With (text*) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "http*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "http*"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "http"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* ('^' || f_unaccent(_Q1_)) )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* ('^' || f_unaccent('http')) )
```

### 7. Ends With (*text) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "*.org"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "*.org"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": ".org"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* (f_unaccent(_Q1_) || '$') )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* (f_unaccent('.org') || '$') )
```

### 8. Literal ('text') (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "'http://example.org'"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "'http://example.org'"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "http://example.org"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') = f_unaccent(_Q1_) )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') = f_unaccent('http://example.org') )
```

### 9. Default (Contains) (lang = 'all')
Param $query_object:
```json
{
    "q": [
        {
            "value": "example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all"
}
```
Parsed result:
```json
{
    "q": [
        {
            "value": "example"
        }
    ],
    "q_operator": null,
    "path": [
        {
            "name": "iri",
            "model": "component_iri",
            "section_tipo": "test3",
            "component_tipo": "test140"
        }
    ],
    "table_alias": "te3",
    "table": "matrix_test",
    "lang": "all",
    "type": "string",
    "params": {
        "_Q1_": "example"
    },
    "sentence": "(te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* f_unaccent(_Q1_) )"
}
```
SQL resolved:
```sql
SELECT * FROM matrix_test AS te3 WHERE 
(te3.section_tipo = 'test3') 
AND (te3.iri @? '$.test140[*]') AND EXISTS ( SELECT 1 FROM jsonb_path_query(te3.iri, '$.test140[*]') AS elem WHERE f_unaccent(elem->>'iri') ~* f_unaccent('example') )
```
