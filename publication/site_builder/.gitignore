# Machine-local config and secrets — never versioned.
.env

# Bun / node
node_modules/

# TypeScript incremental build info
*.tsbuildinfo

# Test scratch roots (created and cleaned by the suite)
.test-tmp/

# The retired pre-instance environment `provision adopt` leaves behind.
.env.pre-instance
