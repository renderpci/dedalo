<?php
$html='';

$html .= "<table class=\"table_list_ts_used\">";

foreach ($ar_rows as $key => $ar_td) {

	$html .= "<tr>";
	
	foreach ($ar_td as $key => $td) {
		$html .= "<td>";
		$html .= $td;
		$html .= "</td>";
	}

	$html .= "</tr>";
}

$html .= "</table>";