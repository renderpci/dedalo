<?php

	# VISUALIZADOR HTML	

	# ELEMENTOS
	if(!isset ($ar_css['css_span_dato'])) 					$ar_css['css_span_dato']='';
	if(!isset ($ar_css['css_autocomplete'])) 				$ar_css['css_autocomplete']='';
	if(!isset ($ar_css['css_span_ejemplo'])) 				$ar_css['css_span_ejemplo']='';
	if(!isset ($ar_css['css_span_debugger']))				$ar_css['css_span_debugger']='';
	if(!isset ($ar_css['css_wrap_autocomplete']))			$ar_css['css_wrap_autocomplete']='';
	
	
	
	echo "\n<!-- AUTOCOMPLETE [tipo:$tipo id_matrix:$id] $label -->";
	#echo "\n<div class=\"css_wrap_autocomplete {$ar_css['css_wrap_autocomplete']}\" >";
	
	# ETIQUETA (LABEL)
	$html_label = component_common_draw::draw_label($this);
	
	# CONTENIDO (INPUT)
	$html_contenido = '';
	switch($permissions) {
			
		case 0	:	$html_contenido .= "\n <span class=\"css_span_dato {$ar_css['css_span_dato']}\"> No access here (autocomplete)! </span>";		
					break;
											
		case ($permissions>=1)	:
										
					$html_contenido .= $valor ;
					
					if(SHOW_DEBUG)
					$html_contenido .= " <span class=\"debug_info\">[$dato]</span>";
						
			if($permissions==3)
			
					$html_contenido .= "\n <span class=\"css_span_debugger {$ar_css['css_span_debugger']}\" id=\"debugger_{$id}\" >{$debugger}</span>";	
					
											
	}
	
	
	# PRINT HTML
	print $html_contenido;
	
	
	#echo "\n</div>\n\n"; //css_wrap_autocomplete
?>