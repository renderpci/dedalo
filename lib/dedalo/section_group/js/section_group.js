// JavaScript Document
$(document).ready(function() {
	
	/*	
	// OBJ SELECTOR
	var section_group_wrap_obj = $('div.css_section_group_wrap');	
	
	// ITERATION TO CONFIGURE SECTION GROUPS STYLE
	// Añade un css específico a los section groups que están dentro de otros, excepto para los que están dentro de un portal
	section_group_wrap_obj.each(function() {
		// Si el section gruoup actual está dentro de otro, le asignamos un estilo específico
		var parent_section_group 	= $(this).parents('div.css_section_group_wrap:first');
		var parent_portal 			= $(this).parents('div.section_edit_in_portal_content:first');	
		if ( $(parent_section_group).length>0  && $(parent_portal).length==0 ) {
			$(this).addClass("css_section_group_wrap_inside");
			if(DEBUG) console.log("section_group inside detected. Added class css_section_group_wrap_inside")
		}
	});
	*/
	
	

	
});






