# Profile overlay example — copy to config/profiles/dev.env and select via
# DEDALO_ENV=dev (env var) or the host map (added in Phase 3). Deltas only:
# list ONLY the keys that differ from defaults.env.
DEVELOPMENT_SERVER=true
DEDALO_MEDIA_ACCESS_MODE=false
